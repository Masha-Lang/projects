#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ifstream input("input.txt");
    ofstream output("output.txt");
    string list, x;
    int last;

    while(getline(input, x)) {
        
    }

    input.close();
    output.close();
    return 0;
}